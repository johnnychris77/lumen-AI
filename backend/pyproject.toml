[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
  "app/app",
  "__pycache__",
  ".venv",
  ".venv-backend",
  "node_modules",
]

[tool.ruff.lint]
select = ["E", "F"]
ignore = [
  "E402",
  "E501"
]
