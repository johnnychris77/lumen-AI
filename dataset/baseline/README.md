# baseline/

Baseline-linkage references for dataset images — which approved
`app.models.baseline_library.BaselineLibraryEntry` a given image's
instrument was compared against at capture time
(`DatasetRegistryEntry.baseline_id`). See
`docs/lcid/DIGITAL_TWIN_LINKAGE.md` for how this composes with the
instrument-identity-based Digital Twin ID on the same entry.
