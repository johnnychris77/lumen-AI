# LPR-DIR-026 — Release Integrity Report (Workstream 6)

Integrity artifacts for the Release Candidate (`main @ 5c22345`). Values below are real
(computed on the RC tree); items that cannot be truthfully produced from a repository are
marked **NOT AVAILABLE** rather than fabricated.

## 1. Version metadata

| Field | Value |
|---|---|
| RC commit | `5c223450b4065011a52ff2dd244c6c5d91321dcc` |
| `git describe` | `lumenai-v1.0.0-rc1-19-g5c22345` |
| Backend Python target | `py312` |
| Frontend `package.json` version field | `0.1.0` (repo has never bumped this app-version field; the release identity is the git SHA + tag, not this field) |
| Valid V1.1 release tag | **None** (`v1.1.0` is stale/divergent — see manifest §4) |

## 2. Build metadata

| Field | Value |
|---|---|
| Integrity run timestamp (UTC) | 2026-07-19T22:41:02Z |
| Source ref | `main` @ `5c22345` |
| Build environment | governance/verification pass (no artifact build performed) |

## 3. Dependency manifest + checksums (real sha256)

| Artifact | sha256 |
|---|---|
| `backend/requirements.txt` | `098cde63713098ae496a79fd6a99464ccba18217c23349e70c4bacfef48c6e85` |
| `backend/requirements-lock.txt` (100 pinned) | `d96ae3a72a4c2ba882c8c9e382fe6fdf4d9858d90a4c31360f85b9686f0dbdde` |
| `frontend/package-lock.json` | `eaa56a3e749b5e3fa5e55360a4f63805cb56920430340dc433fd8cda5eb86c69` |

## 4. Source artifact checksums — V1.1 code delta (real sha256)

| File | sha256 |
|---|---|
| `backend/app/routes/integrations.py` | `1b8cba1d0ee697f50f12a088f5d1d64951646c511e4d253a071cc3f6fa45cf2a` |
| `backend/app/routes/billing.py` | `616366b66f745430e19da86aeae37428d41b2b516f746ba0f3f936802aef997a` |
| `backend/app/routes/billing_webhooks.py` | `0a7b0a079812677e0604bebcbcd8062c62c194c37c4e80eddfc2adb02202011a` |

(Reproduce: `git checkout 5c22345 && sha256sum <file>`.)

## 5. SBOM

| Item | Status |
|---|---|
| Committed SBOMs | `docs/production-readiness/phase-3-security/SBOM.cyclonedx.json`, `docs/pilot-zero/directive-002/sbom/backend-sbom.cdx.json` |
| Currency vs RC | **Backend dependency set is unchanged by V1.1** (the SEC-C-01 fix adds no dependency; it uses stdlib `hmac`/`hashlib`). The RC-pinned dependency set is `requirements-lock.txt` @ the sha256 in §3, which the prior SBOMs enumerate. |
| Freshly regenerated SBOM in this pass | **NOT regenerated by an SBOM tool here** — no `cyclonedx`/`syft` run was performed in this governance pass. The dependency-manifest checksums in §3 are the authoritative RC dependency fingerprint. A CI-generated CycloneDX SBOM should be attached at tag time. |

## 6. Container digests

| Item | Status |
|---|---|
| Backend/frontend image digests | **NOT AVAILABLE.** No container image was built or pushed in this governance pass. `release-ghcr.yml` publishes GHCR images **on a `v*` tag push**, which was deliberately NOT performed (no valid V1.1 tag exists yet; production not authorized). Digests will exist only after an authorized tagged build. Not fabricated. |

## Determination

All integrity artifacts that can be truthfully produced from the repository are provided
with **real checksums** (source delta + dependency manifests) and pinned version/build
metadata. **Container digests are NOT AVAILABLE** (no image built) and a **freshly
tool-generated SBOM was not produced in this pass** — both are honestly marked outstanding
rather than invented. The RC's dependency fingerprint is fully pinned via
`requirements-lock.txt` (sha256 in §3).
