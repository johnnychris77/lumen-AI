# LumenAI Hosted Demo Architecture

## Overview

The LumenAI hosted demo uses a React frontend connected to a hosted FastAPI backend. The current hosted backend is deployed on Render and supports authenticated executive dashboard workflows.

---

## Core Components

