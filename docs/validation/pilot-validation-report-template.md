# Pilot Validation Report — Template

Generated by `GET /api/pilot-validation/report` (pins dataset + model version).
Fill from the live report; keep every honesty caveat.

---

## 1. Study scope
Real-world SPD pilot: AI-assisted inspection vs trained-supervisor ground truth.
Answering: *how well does LumenAI agree with SPD supervisors, especially for
high-risk findings and high-retention zones?*

## 2. Instruments reviewed
- Total reviewed: `{instruments_reviewed}`
- Instrument families: `{instrument_families}`

## 3. Versions
- Dataset version: `{dataset_version}`
- Model id / version: `{model_id}` / `{model_version}`

## 4. Results
- Accuracy / precision / recall / F1: `{results.accuracy}` / `{results.precision}` / `{results.recall}` / `{results.f1}`
- Supervisor agreement rate: `{results.supervisor_agreement_rate}`
- Override rate: `{results.override_rate}`
- False positive / negative rate: `{results.false_positive_rate}` / `{results.false_negative_rate}`
- Confidence calibration: `{results.confidence_calibration}`

## 5. Safety findings
- Critical false-negative rates (blood/tissue/organic residue/crack/missing component): `{safety_findings.safety_metrics}`
- Worst safety FNR: `{results.safety_metrics.worst_safety_false_negative_rate}`
- Safety review queue size: `{safety_findings.safety_queue_size}`

## 6. Zone performance
- Most-commonly-missed zones, highest-risk zones, lowest-confidence zones,
  highest-override zones: `{zone_performance}`

## 7. Go / No-Go
- Decision: `{go_no_go.decision}`
- Blocking issues: `{go_no_go.blocking_issues}`

## 8. Limitations
- Pilot-scale sample; not a validated production model.
- Zone assignment is pilot logic (not CV segmentation).
- Single-supervisor ground truth unless adjudicated.

## 9. Recommendations
`{recommendations}`

## 10. Next training priorities
`{next_training_priorities}`

---
*Decision-support evidence. Human review required. No claim of FDA clearance or
regulatory approval.*
