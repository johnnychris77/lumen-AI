# LumenAI One-Command Demo Control Center

## Purpose

This runbook standardizes the local demo workflow so the project can be started, seeded, checked, stopped, and reset with simple commands.

## Commands

### Start demo

```bash
scripts/demo-start.sh
